{
  "name": "mcp-bridge-plugin",
  "version": "1.0.6",
  "description": "Bridge between Super Productivity and MCP servers",
  "main": "dist/plugin.js",
  "scripts": {
    "build": "tsc",
    "build:plugin": "cat socket.io.min.js plugin-logic.js > plugin.js",
    "package": "npm run build:plugin && cd .. && rm -f mcp-bridge-plugin.zip && cd mcp-bridge-plugin && zip -q -r ../mcp-bridge-plugin.zip manifest.json plugin.js plugin-logic.js socket.io.min.js"
  },
  "keywords": [
    "super-productivity",
    "plugin",
    "mcp"
  ],
  "author": "Alan Delon Sousa Rocha",
  "license": "MIT",
  "dependencies": {
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^20.19.0",
    "typescript": "^5.9.3"
  }
}
